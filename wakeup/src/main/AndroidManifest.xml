<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="me.zalo.startuphelper">

    <application android:supportsRtl="true">
        <receiver
                android:name=".StartupHelperReceiver"
                android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!--<provider-->
        <!--android:name=".StartupHelperProvider"-->
        <!--android:authorities="${applicationId}.provider.startuphelper"-->
        <!--android:enabled="true"-->
        <!--android:exported="true"-->
        <!--tools:ignore="ExportedContentProvider">-->
        <!--<intent-filter>-->
        <!--<action android:name="me.zalo.startuphelper.intent.action.STARTUP_HELPER_PROVIDER" />-->
        <!--</intent-filter>-->
        <!--</provider>-->
    </application>

</manifest>